<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="SysPermissionMapper">
	<select id="findRolePermission" parameterType="java.lang.Integer" resultType="java.util.Map">
		SELECT perm.* FROM tb_sys_permission perm JOIN tb_sys_role_permission role_perm ON perm.id = role_perm.permission_id
		JOIN tb_sys_role role ON role.id = role_perm.role_id
		WHERE role.id = #{roleId}
	</select>
</mapper>